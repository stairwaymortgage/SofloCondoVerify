# dependencies
node_modules
/.pnp
.pnp.js

# next.js
/.next/
/out/
next-env.d.ts

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files — never commit
.env
.env*.local

# Source CSVs holding personal data (names, home addresses, phones).
# This repo is public; these are gated behind RLS in Supabase and must not be
# published here. Keep them locally to run `npm run load:workbooks`.
data/board_contacts.csv
data/cam_licensees.csv

# vercel
.vercel

# typescript
*.tsbuildinfo
